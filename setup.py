#!/usr/bin/env python

from setuptools import setup

if __name__ == "__main__":
    # all the settings are moved to pyproject.toml with
    # minimal setup() calling from the setup.py file as recommended
    # per the latest PyPI documentation on module development
    setup()
